早上到班开始背面试题，复习了前三十道面试题。
今天的第一节课依旧是我们背面试题的时间，
第二节课我们讲了redis，
redis 是一个 C 语言开发的，基于内存的，高性能 key-value 形式的，单线程的 NoSQL数据库。
使用 Redis 的好处有：
(1)速度快，因为数据存在内存中，类似于 HashMap，HashMap 的优势就是查找和操作的时间复杂度都是 O
(2)支持丰富数据类型，支持 string，list，set，Zset，hash 等
(3)支持事务，操作都是原子性，所谓的原子性就是对数据的更改要么全部执行，要么全部不执行
(4)丰富的特性：可用于缓存，消息，按 key 设置过期时间，过期后将会自动删除；
Redis 提供两种持久化机制 RDB 和 AOF 机制:
1）RDB持久化方式： 是指用数据集快照的方式(半持久化模式)记录redis
 数据库的所有键值对,在某个时间点将数据写入一个临时文件，持久化结束后，用这个临时文件替换上次持久化的文件，达到数据恢复。
2）AOF持久化方式： 是指所有的命令行记录以 redis 命令请求协议的格式(完全持久化存储)保存为 aof 文件。
redis端口号是6379
redis支持的数据结构有五种：分别是String list hash set zset
redis一共16384 (0-16383)个哈希槽
下午我们讲了一套机试题，包含增删改查和模糊查。
下午自习课敲了一套机试题，对于导入的后台有点问题，还需多练。
每天的努力每天的收获。